name: Test generate Release

on:
  push:
    branches:
      - main

jobs:
  generate-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Generate Release
        uses: ./ # Use the local path to your action
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
